// TODO: numeric enum
export enum Month {
    // custom index
    JAN = 1,
    FEB,
    MAR,
    APR,
    MEI,
}

// TODO: string enum
export enum Day {
    // custom index
    MON = "Monday",
    TUE = "Tuesday",
    WED = "Wednesday",
    THR = "Thursday",
    FRI = "Friday",
}