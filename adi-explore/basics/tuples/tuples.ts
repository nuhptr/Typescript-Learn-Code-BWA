let myTuple: [string, number, boolean] = ["hello", 10, true]

console.log(myTuple[0]) // "hello"
console.log(myTuple[1]) // 10
console.log(myTuple[2]) // true

// TODO: Destructuring tuples
let myTuple2: [string, number, boolean] = ["hello", 10, true]
let [first, second] = myTuple2
console.log(first) // "hello"
console.log(second) // 10
